// Indicates where the player has been created so it can be used in the different kinds of battle games (local, P2P, Online)
export enum PlayerOrigin {
    LOCAL = 'local',
    REMOTE = 'remote',
    IMPORTED = 'imported'
}