// The base description of a faction
export type Faction = {
    id: string; // local id of the faction
    name: string; // name of the faction. Not modifiable
    image?: string; // image of the faction
};