const STANDARD_CAPTURED_POINTS = 50;

/**
 * Calculates the points awarded for capturing a standard
 * @returns The points awarded for capturing a standard
 */
export function getPointsForStandardCaptured(): number {
    return STANDARD_CAPTURED_POINTS;
}
